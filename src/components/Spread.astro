---
const { spread, homeTeam, awayTeam } = Astro.props;

const team = spread < 0 ? homeTeam : awayTeam;
const adjustedSpread = spread < 0 ? spread : -spread;
---

<div class="text-xs">
  Spread: <span class="font-bold">{ `${team} ${adjustedSpread}` }</span>
</div>
